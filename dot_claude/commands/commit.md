---
allowed-tools: Bash(git add:*), Bash(git status:*), Bash(git commit:*), Bash(git diff:*), Bash(git branch:*), Bash(git log:*), Bash(git show:*), SlashCommand, AskUserQuestion
description: Создать git коммит с автоматической проверкой
---

## Контекст

- Текущий git status: !`git status`
- Текущий git diff (staged и unstaged изменения): !`git diff HEAD`
- Текущая ветка: !`git branch --show-current`
- Последние коммиты: !`git log --oneline -10`

## Твоя задача

Создай git коммит(ы) с автоматической проверкой качества кода через `/stage-check`.

### Процесс работы

#### 1. Анализ изменений

**Если есть staged изменения:**
- Запусти `/stage-check` для полного анализа
- Получи результат с оценкой "Готовность к коммиту"
- Перейди к шагу "Принятие решения"

**Если staged изменений нет, но есть unstaged:**
- Проанализируй все изменения
- Определи логические группы
- Спроси пользователя что добавить в stage (через AskUserQuestion)
- Добавь выбранные файлы в stage
- Запусти `/stage-check`
- Перейди к шагу "Принятие решения"

#### 2. Принятие решения на основе `/stage-check`

**Если "✅ Готово":**
- Используй предложенный commit message из `/stage-check`
- Создай коммит автоматически
- Покажи итоговый результат с хешом коммита

**Если "⚠️ С оговорками":**
- Покажи найденные замечания
- Используй `AskUserQuestion`: "Создать коммит с замечаниями?"
- Если пользователь согласен → создай коммит
- Если нет → останови выполнение, покажи рекомендации

**Если "❌ Не готово":**
- Покажи все критичные проблемы из отчета `/stage-check`
- НЕ создавай коммит
- Предложи использовать `/quick-fix` для автоматического исправления
- Останови выполнение

#### 3. Стратегия для множественных коммитов

Если изменения содержат несколько несвязанных логических групп:

1. **Анализируй изменения для логической группировки**: Просмотри все модификации и определи логически связанные изменения
2. **Разделяй на несколько коммитов когда нужно**: Если изменения затрагивают несколько несвязанных функций или исправляют несколько отдельных проблем, создай отдельные коммиты
3. **Держи связанные изменения вместе**: Изменения, которые зависят друг от друга или реализуют одну функцию, должны быть в одном коммите

**Примеры когда разделять:**
- ✅ Исправление бага в компоненте A + новая функция в компоненте B → 2 коммита
- ✅ Рефакторинг в модуле X + добавление нового API endpoint → 2 коммита
- ✅ Исправление опечатки в документации + реализация бизнес-логики → 2 коммита
- ❌ Добавление нового поля формы + обновление его валидации + добавление UI компонента → 1 коммит (связанная функция)
- ❌ Исправление бага + добавление теста для этого бага → 1 коммит (связанное исправление)

Для разделения используй `git add -p` или конкретные пути к файлам.

#### 4. Создание коммита

Используй следующую команду:
```bash
git commit -m "$(cat <<'EOF'
[commit message from stage-check]
EOF
)"
```

### Формат commit message

Используй commit message предложенный командой `/stage-check`.

**Важные правила:**
- НЕ добавляй co-authors или attribution метаданные
- Сообщение коммита должно быть на английском
- Описание должно объяснять "почему", а не "что"
- Формат: `type(scope): description`

### Формат итогового отчёта

**✅ Коммит создан:**
```
📝 Commit message:
type(scope): description

✅ Коммит успешно создан: [hash]

Результаты проверки:
[краткая выжимка из stage-check]
```

**⚠️ Требуется подтверждение:**
```
⚠️ Найдены замечания:
[список замечаний из stage-check]

Создать коммит несмотря на замечания?
```

**❌ Коммит НЕ создан:**
```
❌ Найдены критичные проблемы:
[список проблем из stage-check]

Рекомендация: используй /quick-fix для автоматического исправления
```

### Важные примечания

- Всегда используй `/stage-check` для проверки перед коммитом
- Не дублируй логику проверок - полагайся на `/stage-check`
- Убирай attribution метаданные из commit message перед созданием
- Если нужно разделить на несколько коммитов, делай это последовательно с проверкой каждого
