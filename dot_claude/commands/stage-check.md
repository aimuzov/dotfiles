---
allowed-tools: Bash(git diff:*), Bash(git status:*), Bash(git log:*), Bash(fish_indent:*), Bash(luacheck:*), Bash(shellcheck:*), Bash(fish:*), Read, Grep, SlashCommand
description: Проверить staged изменения перед коммитом
---

## Контекст

- Текущая ветка: !`git branch --show-current`
- Git статус: !`git status --short`
- Staged изменения: !`git diff --staged`
- Список staged файлов: !`git diff --staged --name-only`

## Твоя задача

**ВАЖНО:** Сначала проверь наличие staged изменений. Если список staged файлов пустой, просто сообщи "Нет staged изменений для проверки" и останови выполнение команды. Не выполняй никакие дальнейшие проверки и не показывай развёрнутый отчёт.

Если staged изменения есть, проверь код в индексе git перед созданием коммита. Дай конкретные рекомендации по улучшению.

### Автоматические проверки

Для staged файлов запусти соответствующие линтеры:

1. **Fish скрипты** (*.fish):
   ```bash
   fish_indent --check <файл>  # проверка форматирования
   fish -n <файл>              # синтаксическая проверка
   ```

2. **Lua файлы** (*.lua):
   ```bash
   luacheck <файл> --globals sbar vim  # проверка если установлен luacheck
   ```

3. **Shell скрипты** (*.sh, без расширения с shebang):
   ```bash
   shellcheck <файл>  # проверка если установлен shellcheck
   ```

4. **Проверка на conventional commits**:
   - Проанализируй staged изменения и предложи commit message по conventional commits
   - Проверь scope из списка в `.commitlintrc.js` (если есть)

5. **Проверка грамматики**:
   - Для markdown файлов (*.md) и файлов с значительным количеством комментариев запусти проверку грамматики через команду `/grammar`
   - Проверь орфографию, пунктуацию и грамматические ошибки в русском и английском тексте
   - Особое внимание на README, документацию, комментарии к функциям

**Важно**: Если линтер не установлен, пропусти проверку и укажи это в отчёте.

### На что обращать внимание

1. **Качество кода**
   - Читаемость и понятность
   - Соблюдение стиля проекта (проверь соседние файлы если нужно)
   - DRY принцип - нет ли дублирования
   - Именование переменных и функций
   - Сложность функций (не слишком ли длинные)

2. **Потенциальные проблемы**
   - Логические ошибки и edge cases
   - Отсутствие обработки ошибок
   - Проблемы с null/undefined
   - Неоптимальные алгоритмы
   - Утечки памяти

3. **Безопасность**
   - SQL injection, XSS, command injection
   - Небезопасная работа с пользовательским вводом
   - Жестко закодированные секреты или пароли
   - Небезопасные зависимости
   - Уязвимые паттерны (eval, innerHTML и т.д.)

4. **Best practices**
   - Соответствие паттернам проекта
   - Правильное использование API
   - Immutability где нужно
   - Async/await vs Promise chains
   - Типизация (если TypeScript)

5. **Специфичные проверки**
   - **Fish shell**: правильное экранирование, использование функций вместо алиасов
   - **Lua (SketchyBar)**: корректная работа с sbar API, обработка событий
   - **Markdown/docs**: правильные ссылки, форматирование
   - **Config файлы**: валидность синтаксиса, полнота настроек

6. **Готовность к коммиту**
   - Нет debug кода (console.log, print и т.д.)
   - Нет закомментированного кода
   - Нет TODO/FIXME без обоснования
   - Нет временных хаков

### Формат ответа

**Статистика изменений:**
- X файлов изменено
- +Y строк добавлено / -Z строк удалено
- Основные затронутые компоненты

**🔍 Результаты линтеров:**
- Список проверенных файлов и результаты
- Ошибки и предупреждения от линтеров

**📖 Проверка грамматики:**
- Список проверенных текстовых файлов (markdown, комментарии)
- Найденные орфографические и грамматические ошибки
- Предложенные исправления

**📝 Предлагаемый commit message:**
```
type(scope): description
```

**✅ Хорошо:**
- Что сделано правильно

**⚠️ Замечания:**
- Что можно улучшить (с указанием файла и строки)
- Конкретные рекомендации по исправлению

**🔴 Проблемы:**
- Критичные проблемы, которые нужно исправить ДО коммита
- Конкретные инструкции как исправить

**💡 Рекомендации:**
- Дополнительные улучшения (можно сделать после коммита)
- Best practices для будущих изменений

**Готовность к коммиту:** ✅ Готово / ⚠️ С оговорками / ❌ Не готово

### Важные примечания

- Анализируй контекст: если изменение в config файле, прочитай весь файл для понимания структуры
- Для shell скриптов обращай особое внимание на безопасность и корректное экранирование
- Проверяй соответствие conventional commits если это применимо
- Для проверки грамматики в markdown файлах и комментариях используй команду `/grammar`, передавая ей соответствующий текст
