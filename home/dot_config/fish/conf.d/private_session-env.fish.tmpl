# vi: ft=fish

# Session variables (including GNUPGHOME and tokens) — single source of truth.
# conf.d/*.fish are sourced BEFORE config.fish, so the variables are available to the whole config.
# Source: home/.chezmoitemplates/session-env.tmpl
{{ includeTemplate "session-env.tmpl" (dict "syntax" "fish" "homeDir" .chezmoi.homeDir) }}
